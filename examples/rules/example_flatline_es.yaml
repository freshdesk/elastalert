kibana_adapter: localhost
kibana_adapter_port: 9200

name: "Heartbeat flatline Example Alert"
type: flatline
index: logstash*

threshold: 10

timeframe:
  hours: 24

# timestamp_field: timestamp
# timestamp_type: 'unix'

filter:
- query:
    query_string:
      query: "response: '200'"

use_count_query: true
doc_type: _doc

alert: 
  - "debug"

